import React from 'react'
import { useForm } from 'react-hook-form'

export const FormDemo4 = () => {
    //  const {register,handleSubmit,gormstate{errors}}=useForm()
  return (
    <div style={{textAlign:'center'}}>
        <h1>hii</h1>
        {/* <form onSubmit={handleSubmit(submithandler)}>

        </form> */}
    </div>
  )
}
